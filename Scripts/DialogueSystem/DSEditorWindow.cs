using UnityEditor;
using UnityEngine;
using UnityEngine.UIElements;

namespace DS.Windows{
    public class DSEditorWindow : EditorWindow
    {
        /*[MenuItem("Window/DialogueSystem/Dialogues Manager")]
        public static void Open() //открытие и создание нового окна
        {
            DSEditorWindow wnd = GetWindow<DSEditorWindow>();
            wnd.titleContent = new GUIContent("Dialogue Graph");
        }
        private void OnEnable(){ //стартовая функция
            AddGraphView();
        }
        private void AddGraphView(){ //добавление графического вида из другого скрипта
            DSGraphView graphView = new DSGraphView();
            
            graphView.StretchToParentSize();

            rootVisualElement.Add(graphView);
        }*/
    }
}