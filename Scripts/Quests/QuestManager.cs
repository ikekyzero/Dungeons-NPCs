using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestManager : MonoBehaviour
{
    /*public Quest[] allQuests; // Все доступные квесты

    public void CheckQuestCompletion(Player player)
    {
        foreach (var quest in allQuests)
        {
            // Проверка условий для завершения квеста
            if (!quest.isCompleted && CanCompleteQuest(player, quest))
            {
                quest.Complete();
                // Добавить награды и обновить активные квесты
            }
        }
    }

    private bool CanCompleteQuest(Player player, Quest quest)
    {
        // Логика проверки выполнения квеста на основе характеристик игрока
        return true; // Вернуть true или false
    }*/
}
